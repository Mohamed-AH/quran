# Production Environment Variables for Render Deployment
# Copy these to Render Dashboard → Environment Variables

# ============================================
# BACKEND ENVIRONMENT VARIABLES
# ============================================

# Server Configuration
NODE_ENV=production
PORT=10000

# Database (MongoDB Atlas)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/hafiz?retryWrites=true&w=majority

# JWT Secrets (GENERATE NEW SECURE KEYS!)
# Use: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_32_CHARS_MINIMUM_SECURE_RANDOM_STRING
JWT_REFRESH_SECRET=CHANGE_ME_32_CHARS_MINIMUM_SECURE_RANDOM_STRING
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# OAuth - Google
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=https://hafiz-backend.onrender.com/api/auth/google/callback

# OAuth - GitHub
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret
GITHUB_CALLBACK_URL=https://hafiz-backend.onrender.com/api/auth/github/callback

# Frontend URL (Update after deploying frontend)
FRONTEND_URL=https://hafiz-frontend.onrender.com

# Security Settings
COOKIE_SECURE=true

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info

# ============================================
# IMPORTANT NOTES
# ============================================
# 1. Replace 'hafiz-backend' and 'hafiz-frontend' with your actual Render service names
# 2. Generate new JWT secrets using: openssl rand -base64 32
# 3. Get MongoDB URI from MongoDB Atlas dashboard
# 4. Update OAuth callback URLs in Google Cloud Console and GitHub
# 5. Never commit these values to Git!
